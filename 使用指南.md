# 职业教育国际化方案生成器 - 使用指南

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 使用方式

#### 方式一: 命令行模式(推荐)

```bash
# 基本用法
python main.py --school "学校名称" --country "合作国家" --major "专业方向"

# 完整示例
python main.py \
  --school "广西现代职业技术学院" \
  --country "印度" \
  --institution "菲律宾南洋理工大学" \
  --major "工业机器人" \
  --output "我的方案.docx"
```

#### 方式二: 交互模式

```bash
python main.py --interactive
```

按提示输入:
- 学校名称
- 合作国家/地区
- 合作院校
- 专业方向
- 项目名称(可选)

#### 方式三: Web界面

```bash
python app.py
```

然后在浏览器访问: http://localhost:5000

### 3. 查看生成的方案

生成的Word文档位于 `output/` 目录:
```
output/学校名称_专业方向_建设方案.docx
```

## 功能说明

### 核心功能

1. **智能内容生成**
   - 自动生成7个标准章节
   - 包含项目背景、建设目标、实施内容、预算等
   - 基于真实方案模板,内容专业规范

2. **灵活配置**
   - 支持自定义学校、国家、专业等参数
   - 自动适配不同合作国家和专业方向
   - 可调整项目名称和输出路径

3. **多格式输出**
   - Word文档(.docx) - 可直接编辑
   - Web预览 - 在线查看方案内容
   - 支持导出PDF(需在Word中转换)

### 生成的方案包含

1. **封面页** - 项目名称、申报单位、合作单位
2. **第1章** - 项目建设的必要性和可行性
3. **第2章** - 项目建设规划
4. **第3章** - 建设内容(详细实施方案)
5. **第4章** - 协同推动职教出海(分工职责)
6. **第5章** - 预期成果
7. **第6章** - 项目预算(含预算表格)
8. **第7章** - 项目保障措施

## 高级用法

### 自定义报价单

如果需要加载实际的报价单数据:

```bash
python main.py \
  --school "XX学院" \
  --pricing "厚溥国际职业教育国际化产品报价单2026版(2.0).xlsx"
```

### 修改默认配置

编辑 `config/settings.py` 文件中的 `DEFAULT_CONFIG`:

```python
DEFAULT_CONFIG = {
    "school_name": "你的学校名称",
    "partner_country": "印度",
    "partner_institution": "合作院校",
    "major": "专业方向",
    ...
}
```

## 常见问题

### Q: 生成的方案在哪里?
A: 在 `output/` 目录下,文件名格式为 `学校名称_专业_建设方案.docx`

### Q: 如何修改方案内容?
A: 生成的Word文档可以直接用Microsoft Word或WPS编辑

### Q: 预算可以调整吗?
A: 可以,在生成的Word文档中直接修改预算表格即可

### Q: 支持哪些合作国家?
A: 理论上支持所有国家,程序会自动适配国家名称。目前针对印度有优化内容。

### Q: 可以批量生成吗?
A: 可以,编写脚本循环调用 `SolutionGenerator` 类即可

## 技术支持

如遇问题,请检查:
1. Python版本 >= 3.8
2. 依赖包是否正确安装
3. 文件路径是否包含特殊字符
4. 是否有写入output目录的权限

## 更新日志

### v1.0 (2026)
- 初始版本发布
- 支持命令行和Web界面
- 完整的7章节方案生成
- 预算表格自动生成
