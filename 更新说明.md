# 方案生成器 v2.0 更新说明

## 新增功能

### 1. 中资企业字段
- ✅ 在配置中添加了"中资企业名称"字段
- ✅ 在Web界面和交互模式中都可以输入合作的中资企业
- ✅ 第4章职责分工会自动包含该企业的职责说明

**使用示例:**
```
中资企业名称：青山控股集团
```

生成的方案中会显示:
```
合作企业（青山控股集团）职责：
1. 提供行业需求分析和岗位标准
2. 参与人才培养方案制定和课程开发
3. 提供实习实训岗位和就业机会
...
```

### 2. 建设内容动态选择
- ✅ 从厚溥国际Excel报价单中加载所有可选产品
- ✅ Web界面提供产品列表勾选功能
- ✅ 支持全选/取消全选
- ✅ 显示每个产品的名称、描述、价格、周期等信息

**使用方法:**
1. 在Web界面点击"加载产品列表"
2. 勾选需要包含在建设内容中的产品
3. 生成的方案第3章会自动包含这些产品
4. 第6章预算会根据选择的产品自动生成

### 3. 自定义建设内容
- ✅ 支持添加不在Excel中的自定义项目
- ✅ 可以输入项目名称、描述、预算金额
- ✅ 支持添加多个自定义项目
- ✅ 自定义项目会合并到建设内容和预算中

**使用方法:**
1. 在Web界面点击"+ 添加自定义项目"
2. 填写项目名称、描述、预算金额
3. 可以添加多个自定义项目
4. 可以随时删除不需要的项目

### 4. 基于Excel的智能预算
- ✅ 预算表格自动从Excel报价单提取真实价格
- ✅ 根据选择的产品自动生成对应预算项
- ✅ 自定义项目的预算也会合并到总预算中
- ✅ 如果没有选择任何产品,使用默认预算模板

**预算生成逻辑:**
- 有选择产品 → 使用Excel中的实际价格
- 有自定义项目 → 使用输入的预算金额
- 都没有 → 使用默认预算模板(10项标准预算)

## 改进的功能

### 1. Excel解析增强
- 更完整的产品信息提取
- 自动产品分类
- 价格和周期的准确解析
- 错误处理更健壮

### 2. 内容构建优化
- 第三章建设内容支持动态生成
- 根据选择的产品生成对应的实施内容
- 保留原有的7章节标准结构

### 3. Web界面优化
- 更友好的产品选择界面
- 实时显示已选产品信息
- 自定义项目的可视化管理
- 响应式设计,更易用

## API接口

### 新增API端点

**GET /api/products**
- 获取所有可选产品列表
- 返回JSON格式的产品数据
- 包含: id, name, description, price, unit, category, duration

**示例响应:**
```json
[
  {
    "id": "1",
    "name": "海外院校对接服务",
    "description": "1. 海外开发学校...",
    "price": "5",
    "unit": "项",
    "category": "院校对接",
    "duration": "30天~60天"
  }
]
```

## 技术改进

### 后端
1. **Excel解析器** (`parsers/excel_parser.py`)
   - 新增 `_categorize_product()` 方法
   - 新增 `get_all_products_for_selection()` 方法
   - 改进价格解析逻辑

2. **内容构建器** (`generators/content_builder.py`)
   - 新增 `chinese_enterprise` 配置项
   - 新增 `selected_items` 和 `custom_items` 参数
   - 新增 `_generate_dynamic_content()` 方法
   - 改进 `_generate_budget_items()` 支持动态预算

3. **Web服务** (`app.py`)
   - 新增 `/api/products` 端点
   - 改进表单数据处理
   - 支持JSON格式的产品和自定义项目
   - 单例模式的Excel解析器

### 前端
1. **首页** (`templates/index.html`)
   - 新增中资企业输入框
   - 新增产品选择区域
   - 新增自定义项目管理
   - 改进JavaScript逻辑

2. **预览页** (`templates/preview.html`)
   - 显示中资企业信息
   - 保持原有预览功能

## 使用场景

### 场景1: 快速生成标准方案
不选择任何产品,使用默认模板
```bash
python main.py --school "XX学院" --country "印度" --major "工业机器人"
```

### 场景2: 定制化方案
通过Web界面选择具体产品和添加自定义内容
1. 访问 http://localhost:5000
2. 填写基本信息(包括中资企业)
3. 加载产品列表并勾选需要的
4. 添加自定义项目(可选)
5. 点击"生成并下载"

### 场景3: 批量生成不同配置
使用命令行模式循环生成
```python
from main import SolutionGenerator

configs = [
    {"school_name": "学校A", "major": "工业机器人"},
    {"school_name": "学校B", "major": "新能源汽车"},
]

for config in configs:
    gen = SolutionGenerator(config)
    gen.generate()
```

## 兼容性说明

- ✅ 完全兼容v1.0的命令行用法
- ✅ 保留所有原有的7章节结构
- ✅ 默认行为保持不变
- ✅ 旧的使用方式仍然有效

## 已知限制

1. Excel文件编码问题可能导致中文显示乱码(已在代码中处理)
2. 产品价格必须是数字或可解析的字符串
3. 自定义项目目前只支持简单的文本描述
4. AI内容生成暂未集成(计划中)

## 下一步计划

- [ ] 集成AI/网络搜索生成第1章必要性分析
- [ ] 支持更多国家的专业化内容
- [ ] 添加PDF导出功能
- [ ] 增加方案模板选择
- [ ] 支持在线编辑保存
- [ ] 添加方案评估功能

---

**更新时间**: 2026年
**版本**: v2.0
